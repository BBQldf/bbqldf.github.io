---
layout:     post
title:      年会攻略生成器
subtitle:   用 AI 辅助生成活动方案和年会玩法的产品小记
date:       2025-12-02
author:     bbq
header-img: img/post-bg-meeting-room.jpg
catalog: true
tags:
    - AI
    - 产品
    - 工具
---

# 年会攻略生成器

通过从Google、baidu、xiaohongshu等搜索引擎查询常见的公司年会安排，根据员工调研，生成周密完整的年会策划安排。

## 概述

此技能通过以下方式自动创建详细的旅行指南：

- 搜索并提取小红书的年会笔记（如果需要登陆，需要反馈）
- ⚠️ **从小红书收集热门年会安排的详细信息（必需）**
  - xiaohongshu链接
  - 评分和评论数
  - 年会计划
  - 📸 **图片截图（必需）**
- 🗺️ **完整的年会时间线**
- 将所有内容编译成结构化的 Markdown

**核心质量保证**：

- ✅ 每个年会安排点都必须有数据支撑
- ✅ 两天的行程安排
- ✅ 完整时间表保证日程的可控
- ✅ 节目单需标注时长、负责人、所需道具 
- ✅ 供应商信息必须包含联系方式和评分
- ✅ 应急方案需覆盖5类常见风险（设备/人员/流程/安全/天气）

**依赖要求**:

- Chrome DevTools MCP 服务器（推荐，用于浏览器自动化）
- 或 Web Search + Web Reader MCP 工具（替代方案）

## 收集员工意愿

首先从员工那里收集基本信息，了解其想要的年会安排。

\### 必需参数

- ***\*公司规模\**** (Company Size): 参与人数（如200人）
-  ***\*预算范围\**** (Budget): 总预算（如10-15万）
-  ***\*年会类型\**** (Event Type): 晚宴型/旅游型/混合式

### 可选参数

- ***\*主题偏好\**** (Theme): 如"科技未来""复古派对" 
- ***\*特殊需求\**** (Special Needs): 如"需直播""需抽奖系统"

```
用户: "策划一个200人、预算12万的科技主题年会"
收集信息: size=200, budget=12万, theme=科技
```

```
## 方案生成

### 1. 预算表（3档方案）
```markdown
| 项目       | 基础版(8万) | 标准版(12万) | 豪华版(15万) |
|------------|------------|-------------|-------------|
| 场地       | 3万        | 5万         | 7万         |
| 餐饮       | 2万        | 3万         | 4万         |
| 演艺       | 1万        | 2万         | 3万         |
| 奖品       | 1万        | 1.5万       | 2万         |
| **总计**   | **7万**    | **11.5万**  | **16万**    |
```



## 搜索小红书

从小红书提取真实的旅行体验和推荐。

### 步骤1: 访问小红书

使用 `navigate_page` 访问 xiaohongshu.com 并等待页面加载:

```
navigate_page("https://www.xiaohongshu.com")
wait_for(".search-input", 5000)
```

### 步骤2: 搜索年会笔记

在搜索框中填入公司类型和年会风格:

```
fill(".search-input", "{destination} {trip_type} 年会")
press_key("Enter")
wait_for(".note-item", 10000)
```

### 步骤3: 提取笔记内容

使用 `evaluate_script` 从搜索结果中提取结构化数据:

- 标题、作者、内容文本
- 点赞数、收藏数
- 提到的日程时间点和具体安排
- 年会提示和实用信息建议

详细的提取模式，请参考 `references/xiaohongshu-selectors.md`。

### 步骤4: 加载更多结果

向下滚动加载更多笔记:

```
for i in range(3):
    press_key("End")
    wait_for(2000)
```

### 步骤5: 浏览至少10篇笔记 (必需)

**关键**: 你必须浏览并提取至少10篇相关年会笔记的内容。不要跳过此步骤或匆忙完成。

1. **点击每篇笔记**在搜索结果中

2. **提取完整内容**从每篇笔记中:

   - 完整的行程/路线
   - 所有提到的景点、餐厅、酒店
   - 价格和预算信息
   - 实用提示和警告
   - 营业时间和联系信息
   - 个人经验和推荐

3. **返回并继续**下一篇笔记:

   ```
   navigate_page("back")
   wait for page load
   click next note link
   ```

4. **重复直到浏览了至少10篇笔记**:

   - 重点关注互动量高的笔记（点赞/收藏）
   - 优先选择最近的帖子（最近6个月内）
   - 包含不同视角（公司文化、团队建设、节目舞蹈、互动游戏）
   - 花时间仔细阅读 - 质量优于速度

5. **追踪你的进度**:

   - 记录浏览的笔记数量
   - 记录提到的独特地点
   - 记录多篇文章中的共同推荐
   - 识别价格范围和预算提示

**重要提示**:

- ⚠️ 在浏览至少10篇笔记之前不要生成行程
- ⚠️ 即使笔记看起来相似也不要跳过 - 每篇都可能有独特见解
- ⚠️ 最终行程的质量取决于充分的研究
- 时间投入: 浏览最少需要10-15分钟

### 步骤6: 处理提取的数据

- 按标题和内容相似性去重笔记
- 按点赞数和收藏数排序
- 提取关键信息（车程、晚宴、节目表演）
- 识别共同主题和推荐
- 创建所有提到地点的汇总列表

## 整合Google、Baidu搜索引擎 (必需步骤)

**⚠️ 重要**: 每个景点和餐厅都必须通过 Google Maps MCP 搜索获取详细信息。这是生成高质量攻略的必需步骤，不可跳过。

从Google Maps获取详细信息以丰富提取的地点。

### 对于常见公司年会提取数据 (必须执行)

1. **在Google Maps上搜索**

   ```
   navigate_page("https://www.google.com/maps")
   wait_for(".searchbox", 5000)
   fill(".searchbox", "{location_name}")
   press_key("Enter")
   wait_for(".place-info", 10000)
   ```

2. **提取地点详情 (必需)**
   使用 `evaluate_script` 检索以下所有信息:

   - ✅ 完整地址 (必需)
   - ✅ Google Maps直接链接 (必需)
   - ✅ 评分和评论数 (必需)
   - 营业时间
   - 电话号码
   - 网站URL
   - **💳 支付方式** (是否接受信用卡/借记卡，这是必需信息)

3. **保存地点图片截图 (必需)**

   ```
   take_screenshot("./maps/{location_id}.png", fullPage=False)
   ```

   **截图要求**:

   - ⚠️ 使用Chrome DevTools MCP的`take_screenshot`工具
   - 截图应包含地点的街景视图、地图标记或地点照片
   - 确保截图清晰可见,能识别地点特征
   - 图片将嵌入到最终攻略中
   - 文件命名格式: `{地点名称英文名或拼音}.png`

**重要提示**:

- ⚠️ 每个提到的景点都必须在 Google Maps 上搜索
- ⚠️ 必须提取完整的 Google Maps 链接
- ⚠️ 必须提取评分和评论数
- ⚠️ 这些信息必须出现在最终生成的攻略中

详细的Google Maps工作流程，请参考 `references/google-maps-workflows.md`。

### 使用 MCP 工具的替代方法

如果 Chrome DevTools MCP 服务器不可用，可以使用以下 MCP 工具：

**Web Search + Web Reader 组合**:

1. 使用 `mcp__web-search-prime__webSearchPrime` 搜索地点信息
2. 使用 `mcp__web_reader__webReader` 获取详细地址和评分信息

**示例流程**:

```python
# 搜索景点信息
search_result = mcp__web-search-prime__webSearchPrime(
    search_query="Tan Dinh Church 胡志明市 评分 地址",
    location="cn"
)

# 从搜索结果中提取 Google Maps 链接
maps_link = extract_google_maps_link(search_result)

# 获取详细信息
details = mcp__web_reader__webReader(
    url=maps_link,
    return_format="markdown"
)
```

## 生成Markdown行程

## 输出格式

### 1. Markdown方案

- 包含：预算表、节目单、供应商列表、应急方案
- 示例：`examples/annual-party-2026.md`

### 2. 交互式甘特图

- 使用HTML+JS生成，支持：
  - 拖拽调整时间节点
  - 点击查看供应商详情
  - 实时预算计算

## 错误处理

### 常见场景

**小红书需要登录**

- 通知用户可能需要登录
- 考虑现有缓存数据是否足够
- 如需要，使用替代数据源

**Google Maps上找不到地点**

- ⚠️ 这是关键步骤，必须尝试多种方法
- 尝试简化的搜索词
- 如果城市名称多余则删除
- 按景点类别+地标搜索
- 使用中英文混合搜索
- 如果仍然找不到，使用 Web Search + Web Reader 替代方案
- ⚠️ 在最终文档中明确标注"Google Maps 信息未找到"

**无法提取评分或链接**

- 检查网络连接
- 尝试刷新页面重新搜索
- 使用替代的 Google Maps 搜索工具
- 如果持续失败，记录并在文档中说明

**页面加载缓慢**

- 增加wait_for超时值
- 使用take_snapshot验证页面状态
- 如果性能关键，考虑提取较少数据
- 对于 Google Maps，确保等待 .place-info 元素加载完成

**访问频率限制**

- 在搜索之间添加延迟（2-3秒）
- 限制滚动迭代
- 缓存结果以避免重复搜索
- 对于 Google Maps，避免短时间内多次搜索相同地点

## 完整工作流程示例

```
1. 参数收集
用户输入：
"策划一个200人、预算12万的科技主题年会，需要抽奖系统，场地在上海"
{
  "company_size": 200,
  "budget": "12万",
  "theme": "科技",
  "special_req": "抽奖系统",
  "location": "上海"
}
2. 搜索公开供应商（替代公司内部库）
指令：使用Web搜索获取供应商（类似小红书搜索）
for category in ["演艺", "餐饮", "场地"]:
    # 搜索关键词组合
    search_query = f"{params['location']} 年会{params['theme']}{category} 评分4.0以上"
    
    # 调用Web搜索MCP
    suppliers[category] = mcp__web-search__search(
        query = search_query,
        filters = {
            "rating_min": 4.0,
            "location": params["location"]
        }
    )
    
    # 提取结构化数据（类似小红书笔记提取）
    for supplier in suppliers[category]:
        supplier["contact"] = extract_phone(supplier["description"])  # 提取电话
        supplier["contract"] = find_contract_template(supplier["name"])  # 搜索合同模板
        supplier["rating"] = extract_rating(supplier["description"])    # 提取评分
输出：

供应商	类型	报价	评分	联系方式	数据来源
XX科技	演艺	3万	4.8	138XXXX8888	大众点评+官网
YY餐饮	餐饮	4万	4.2	139XXXX9999	美团+知乎评测
ZZ场地	场地	5万	4.5	137XXXX6666	携程+场地租赁平台
3. 员工调研（公开数据替代）
指令：搜索行业报告/问卷（替代公司内部调研）
survey_data = mcp__web-search__search(
    query = "2026年公司年会员工偏好调研 行业报告",
    filters = {
        "source": ["知乎", "艾瑞网", "36氪"],  # 优先权威来源
        "date": "2024-2026"                   # 近2年数据
    }
)

# 提取关键结论（类似小红书笔记提取）
key_findings = {
    "program_preference": extract_keyword(survey_data, "节目偏好"),  # 如"70%选择互动游戏"
    "dietary_restrictions": extract_keyword(survey_data, "饮食禁忌"), # 如"素食者占比15%"
    "taboos": extract_keyword(survey_data, "禁忌")                   # 如"避免政治话题"
}
4. 节目创意生成（公开数据）
指令：
# 搜索科技主题年会案例（类似景点搜索）
program_ideas = mcp__web-search__search(
    query = f"{params['theme']}主题年会 创意 2026",
    filters = {
        "source": ["小红书", "知乎", "活动汪"],
        "sort": "popularity"  # 按热度排序
    }
)

# 提取TOP3节目（类似景点评分）
top_programs = []
for program in program_ideas[:3]:
    top_programs.append({
        "name": program["title"],
        "rating": extract_popularity(program["likes"]),  # 用点赞数替代评分
        "supplier": match_supplier(program["title"], suppliers["演艺"]),  # 匹配供应商
        "risk": extract_risk(program["description"])     # 提取风险提示（如"需提前调试"）
    })
输出：

节目名称	热度	供应商	风险提示
AI虚拟偶像合唱	9.2	XX科技	需提前1小时调试设备
元宇宙抽奖	8.7	XX科技	需测试网络稳定性
全息投影秀	8.5	YY演艺	需确认场地层高
5. 预算生成（公开数据）
指令：搜索行业基准数据（替代历史数据）
budget_benchmark = mcp__web-search__search(
    query = f"{params['company_size']}人年会 预算 行业报告",
    filters = {
        "source": ["艾瑞网", "活动汪", "知乎"],
        "date": "2024-2026"
    }
)

# 生成3档预算（类似旅行攻略的预算表）
budget_table = {
    "基础版": calculate_budget(budget_benchmark, "low"),
    "标准版": calculate_budget(budget_benchmark, "medium"),
    "豪华版": calculate_budget(budget_benchmark, "high")
}
输出：

项目	基础版(8万)	标准版(12万)	豪华版(15万)	行业基准
场地	3万	5万	7万	4.5万
餐饮	2万	4万	5万	3.8万
演艺	1万	3万	4万	2.5万
6. 应急方案（公开数据）
指令：

python
复制
# 搜索年会风险案例（类似景点应急）
emergency_cases = mcp__web-search__search(
    query = "公司年会 突发状况 应对方案",
    filters = {
        "source": ["知乎", "活动汪", "小红书"],
        "sort": "relevance"
    }
)

# 提取TOP5风险（类似旅行攻略的应急库）
emergency_plan = []
for case in emergency_cases[:5]:
    emergency_plan.append({
        "risk": case["title"],
        "solution": extract_solution(case["description"]),
        "frequency": extract_frequency(case["comments"])  # 用评论数替代频率
    })
    
    
# 生成Markdown方案（类似旅行攻略）
mcp__markdown_generator__create(
    file_path = "./output/年会方案_2026.md",
    budget_table = budget_table,
    program_list = top_programs,
    emergency_plan = emergency_plan,
    suppliers = suppliers
)

# 生成交互式甘特图（HTML）
mcp__gantt_generator__create(
    tasks = top_programs,
    suppliers = suppliers,
    output_path = "./output/年会甘特图_2026.html"
)
```

## 附加资源

### 参考文件

详细的实施指导:

- **`references/xiaohongshu-selectors.md`** - 完整的DOM选择器、JavaScript提取模式和处理小红书的动态内容加载
- **`references/google-maps-workflows.md`** - Google Maps导航、地点数据提取、路线生成和截图捕获方法
- **`references/data-extraction-patterns.md`** - 数据清理、去重、模糊名称匹配和预算计算算法
- **`references/markdown-templates.md`** - 不同旅行类型的详细模板（美食、文化、自然）、特殊格式和丰富内容嵌入

